services:
  frontend:
    build: ./frontend

  backend:
    build: ./backend
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    volumes:
      - mongo_data:/data/db

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend

volumes:
  mongo_data: